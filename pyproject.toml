[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mec-cast-logging-service"
version = "0.1.0"
description = "A logging service for collecting, storing, and querying logs from mec-cast applications."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "asyncpg>=0.30",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.7",
]

[project.scripts]
mec-cast-logs = "mec_cast_logging.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/mec_cast_logging"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
